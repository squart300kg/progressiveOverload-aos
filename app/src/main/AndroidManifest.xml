<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.program">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.home.sub.TimerActivity"
            android:exported="true" />
        <activity
            android:name=".ui.record.sub.OneDayRecordActivity"
            android:exported="true" />
        <activity
            android:name=".ui.record.sub.RecordDetailActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.sub.RecordExerciseActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.sub.SplitSelectionActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.sub.ExerciseTypeActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.sub.RegExerciseTypeDetailActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>